# ---------------------------------------------------------------
# Emotional Weight — Backend Dependencies
# ---------------------------------------------------------------
# 1. Create venv:   python -m venv backend/venv
# 2. Activate:      backend\venv\Scripts\activate   (Windows)
#                   source backend/venv/bin/activate (Linux/Mac)
# 3. Install:       pip install -r backend/requirements.txt
# 4. Install TRIBE: pip install -e git+https://github.com/facebookresearch/tribev2.git#egg=tribev2
# ---------------------------------------------------------------

# Web framework
fastapi==0.111.0
uvicorn[standard]==0.29.0

# Sentence tokenisation
nltk==3.8.1

# Numerical computing (vertex activation arrays)
numpy==1.26.4
scipy==1.13.0
pandas==2.2.2

# Audio processing (dependencies for TRIBE's internal TTS)
soundfile==0.12.1

# NIfTI / GIFTI atlas parsing
nibabel==5.2.0

# HuggingFace Hub — for downloading model weights at startup
huggingface-hub==0.23.0

# Pytest for test suite (Phase 3)
pytest==8.2.0
